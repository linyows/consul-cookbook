consul = "127.0.0.1:8500"
retry = "10s"
max_stale = "10m"
log_level = "warn"
pid_file = "/var/run/consul-template/pid"

syslog {
  enabled = true
  facility = "LOCAL5"
}

template {
  source = "/tmp/nginx.backends.ctmpl"
  destination = "/tmp/dynamic.backends.conf"
  command = "service nginx reload"
}
